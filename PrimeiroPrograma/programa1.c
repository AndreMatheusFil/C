#include <stdio.h>

int main(){
	//declarando variáveis
	int idade;
	int idadeinacializada = 0;

	//Função para escrever algo na saída padrão (console)
	printf("Qual é a sua idade?\n");
	//função para receber dados do teclado %d é para inteiro
	scanf("%d",&idade);

	return 0;
}
